services:
  - type: web
    name: theraquest-api
    env: python
    rootDir: server
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python run.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
